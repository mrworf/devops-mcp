export const MCP_INSTRUCTIONS = [
  "This MCP server lets agents call configured HTTP services without exposing raw credentials. Always call list_services first, then request_tokens with a clear reason, then use service_request with service, destination, method, path or allowed URL, headers/body containing opaque tokens, and a request reason. Tokens are not real credentials and only work through this MCP server. Requests may be denied by service policy. Response secrets may be returned as service-scoped sec_ tokens. Cookie headers and caller-supplied Transfer-Encoding are not supported.",
  "Never claim an opaque token is a real API key. Never attempt to use opaque tokens outside this MCP server. Use relative paths when possible. Use absolute URLs only when they match a configured destination. Include a specific reason for every token request and service request. If denied, call explain_denial. Do not retry denied requests by changing hosts, paths, or token placement unless the denial explanation says it is allowed. Treat TLS verification warnings as meaningful risk signals. Do not ask the user for raw credentials.",
].join(" ");
