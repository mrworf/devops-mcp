export const MCP_INSTRUCTIONS = [
  "This MCP server lets agents call configured HTTP services without exposing raw configured credentials. Secret isolation is enforced by the gateway backend before content reaches you; it does not rely on you recognizing or keeping secrets confidential. Always call list_services first, then request_tokens with a clear reason, then use service_request with service, destination, method, path or allowed URL, headers/body containing opaque tokens, and a request reason.",
  "gref_ values are temporary placeholders, not credentials. They are bound to the authenticated subject, originating service, destination, and credential, have idle and maximum lifetimes, and work only through this gateway. Downstream responses are scanned on the backend before delivery; detected secrets are replaced with sec_ placeholders bound to the authenticated subject and originating service. Subject binding survives MCP transport reinitialization; mcp-session-id is not an authorization boundary.",
  "Never claim an opaque token is a real API key, reveal it as a credential, or attempt to use it outside this MCP server. Use relative paths when possible. Use absolute URLs only when they match a configured destination. Include a specific reason for every token request and service request. Requests may be denied by service policy. If denied, call explain_denial. Do not retry denied requests by changing hosts, paths, or token placement unless the denial explanation says it is allowed. Cookie headers and caller-supplied Transfer-Encoding are not supported. Treat TLS verification warnings as meaningful risk signals. Do not ask the user for raw credentials.",
].join(" ");
