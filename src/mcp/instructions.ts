export const MCP_INSTRUCTIONS = [
  "This MCP server lets agents call configured HTTP services without exposing protected backend values. Secret isolation is enforced by the gateway backend before content reaches you; it does not rely on you recognizing or keeping secrets confidential. Always call list_services first, then get_gateway_service_references with a clear reason, then use service_request with service, destination, method, path or allowed URL, headers/body containing gateway references, and a request reason.",
  "gref_ values are temporary gateway service references, not credentials. They are bound to the authenticated subject, originating service, destination, and access entry, have idle and maximum lifetimes, and work only through this gateway. Downstream responses are scanned on the backend before delivery; detected secrets are replaced with sec_ references bound to the authenticated subject and originating service. The MCP transport is stateless; reference continuity comes from authenticated-subject binding rather than transport state.",
  "Never claim a gateway reference is a real API key, reveal it as a credential, or attempt to use it outside this MCP server. Use relative paths when possible. Use absolute URLs only when they match a configured destination. Include a specific reason for every reference request and service request. Requests may be denied by service policy. If denied, call explain_denial. Do not retry denied requests by changing hosts, paths, or reference placement unless the denial explanation says it is allowed. Cookie headers and caller-supplied Transfer-Encoding are not supported. Treat TLS verification warnings as meaningful risk signals. Do not ask the user for raw credentials.",
].join(" ");
